import topics from './topics';
import users from './users';
import accounts from './accounts';
import repositories from './repositories';

export { topics, users , accounts};

export default {
  topics,
  users,
  accounts,
  repositories
};
